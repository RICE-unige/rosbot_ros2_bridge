# Student Configuration Template
# Copy this to .env and update with your values
# Or run ./connect.sh for interactive setup

# ROS1 Configuration - ROSbot connection
ROS_MASTER_URI=http://10.42.0.18:11311  # ROSbot IP address
ROS1_IP=10.42.0.1                        # Jetson IP address

# ROS2 Configuration
ROS_DOMAIN_ID=27                         # Must match on all ROS2 devices
RMW_IMPLEMENTATION=rmw_fastrtps_cpp      # FastDDS (recommended) or rmw_cyclonedds_cpp
